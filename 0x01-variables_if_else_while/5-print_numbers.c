#include <stdio.h>

/**
 * main - A program that prints the alphabet in lowercase and uppercase
 *
 * Return: Always 0.
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
