#include "main.h"

/**
 * _abs - This program would compute the absolute value of an integer
 * @t: The integer to be computed
 * Return: This program would return the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
